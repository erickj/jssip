goog.provide('jssip.ParserRegistrySpec');

goog.require('jssip.ParserRegistry');

describe('SIP Parser', function() {
  it('should register header handlers', function() {
    var parserRegistry = new jssip.ParserRegistry();
  });
});
