goog.provide('jssip.ParserManagerSpec');

goog.require('jssip.ParserManager');

describe('SIP Parser', function() {
  it('should register header handlers', function() {
    var parserManager = new jssip.ParserManager();
  });
});
