goog.provide('jssip.ParserSpec');

goog.require('jssip.Parser');

describe("SIP Parser", function() {
  it("should register header handlers", function() {
    var parser = new jssip.messaging.Parser();
  });
})